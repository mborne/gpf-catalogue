{
  "$comment": "Author: Claude (Anthropic) — this schema is AI generated and exported from gpf_catalogue.model by scripts/export_schema.py; do not edit it by hand.",
  "$defs": {
    "Extent": {
      "additionalProperties": false,
      "description": "One geographic extent of a resource, with the name the catalogue gave it.\n\nA record often declares one extent per territory it covers: `IGNF_BD-TOPO`\npublishes eight, from \"France métropolitaine\" to \"Saint-Martin\", each with its\nown box and its ISO 3166 alpha-3 code. `CatalogueRecord.bbox` is their union,\nwhich for that record spans the Atlantic and the Indian Ocean; this is the\ndetail that says what is actually covered.\n\nAttributes:\n    name: Label published for the zone, e.g. \"Guadeloupe\".\n    code: Code identifying the zone, e.g. \"GLP\".\n    code_space: Title of the authority the code belongs to.\n    bbox: The zone's own box, `[west, south, east, north]` in decimal degrees.",
      "properties": {
        "name": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Label published for this zone, e.g. 'Guadeloupe'. Null when the catalogue names the extent without describing it, which is the case of 167 of the 580 extents of the catalogue.",
          "title": "Name"
        },
        "code": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Code identifying the zone, e.g. 'GLP'. Null when the extent carries no geographic identifier.",
          "title": "Code"
        },
        "codeSpace": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Title of the authority `code` belongs to, e.g. 'ISO 3166 alpha 3'. Null when the identifier cites none.",
          "title": "Codespace"
        },
        "bbox": {
          "description": "Box of this zone alone, as [west, south, east, north] in decimal degrees (WGS 84), in GeoJSON order. An extent whose box is absent or half declared is not published at all, rather than completed.",
          "items": {
            "type": "number"
          },
          "title": "Bbox",
          "type": "array"
        }
      },
      "required": [
        "bbox"
      ],
      "title": "Extent",
      "type": "object"
    },
    "Link": {
      "additionalProperties": false,
      "description": "An access endpoint of a resource.\n\nAttributes:\n    type: What the link leads to.\n    url: The absolute URL, as published by the catalogue.\n    name: Label given by the catalogue — often a layer, `BDTOPO_V3:batiment`.\n    description: Human readable label of the same entry, \"BD TOPO® V3 batiment\".",
      "properties": {
        "type": {
          "$ref": "#/$defs/LinkType",
          "description": "What the link leads to."
        },
        "url": {
          "description": "Absolute URL of the endpoint.",
          "title": "Url",
          "type": "string"
        },
        "name": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Label published for this entry, null when the catalogue gives none. On a service it is usually the layer or feature type reached at that URL, e.g. `BDTOPO_V3:batiment`.",
          "title": "Name"
        },
        "description": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Human readable label of the same entry, e.g. 'BD TOPO® V3 batiment'. Null when the catalogue gives none; it differs from `name` on 2 212 of the 2 243 entries carrying both.",
          "title": "Description"
        }
      },
      "required": [
        "type",
        "url"
      ],
      "title": "Link",
      "type": "object"
    },
    "LinkType": {
      "description": "What a link leads to.\n\nThe catalogue leaves `cit:protocol` empty on 85 % of its links, so the type is\ninferred from the URL when the protocol is missing (see `gpf_catalogue.parse`).\nThe point of typing is that a consumer can act on a link without opening it.",
      "enum": [
        "wfs",
        "wms",
        "wmts",
        "tms",
        "download",
        "capabilities",
        "documentation",
        "other"
      ],
      "title": "LinkType",
      "type": "string"
    },
    "ResourceType": {
      "description": "Kind of resource described by a metadata record.\n\nValues mirror the ISO `MD_ScopeCode` code list, restricted to the values\nactually used by the Géoplateforme catalogue.",
      "enum": [
        "dataset",
        "series",
        "service"
      ],
      "title": "ResourceType",
      "type": "string"
    },
    "SpatialScope": {
      "description": "Extent a resource is meant to describe, from the INSPIRE code list.\n\nValues are the last segment of the code list URI\n`http://inspire.ec.europa.eu/metadata-codelist/SpatialScope`, which the\ncatalogue publishes as the `xlink:href` of a keyword anchor. They answer\n\"is this a national product or a local one?\", which is the cheapest filter\na search can apply before looking at a bounding box.",
      "enum": [
        "global",
        "european",
        "national",
        "regional",
        "local"
      ],
      "title": "SpatialScope",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "A resource of the Géoplateforme catalogue, flattened.\n\nEvery field but `file_identifier` and `type` is optional, because the catalogue\npublishes records that lack almost anything. A field the catalogue does not\nprovide is `None` or an empty list, never invented.",
  "properties": {
    "fileIdentifier": {
      "description": "Stable identifier of the metadata record, usable to fetch it again from the CSW service or to build its cartes.gouv.fr URL.",
      "title": "Fileidentifier",
      "type": "string"
    },
    "type": {
      "$ref": "#/$defs/ResourceType",
      "description": "Kind of resource described by the record."
    },
    "title": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Human readable name of the resource, null when it has none.",
      "title": "Title"
    },
    "abstract": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Free text description of the resource, null when it has none.",
      "title": "Abstract"
    },
    "edition": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Version of the resource as its citation states it, e.g. '3.5' for BD TOPO®. Read from the resource citation only: `cit:edition` also appears in every distribution format citation, where it says 'inapplicable'. Null when the citation states none.",
      "title": "Edition"
    },
    "producer": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Organisation responsible for the resource, read from its point of contact. Null when the record names none.",
      "title": "Producer"
    },
    "contactEmail": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Contact email address of the producer, null when absent.",
      "title": "Contactemail"
    },
    "keywords": {
      "description": "Free and controlled keywords, deduplicated, in the order the record publishes them. Empty when the record carries none.",
      "items": {
        "type": "string"
      },
      "title": "Keywords",
      "type": "array"
    },
    "inspireThemes": {
      "description": "Keywords coming from the GEMET INSPIRE themes thesaurus, a controlled vocabulary usable as a search facet. A subset of `keywords`.",
      "items": {
        "type": "string"
      },
      "title": "Inspirethemes",
      "type": "array"
    },
    "topicCategories": {
      "description": "ISO 19115 topic categories (`MD_TopicCategoryCode`), e.g. `environment`.",
      "items": {
        "type": "string"
      },
      "title": "Topiccategories",
      "type": "array"
    },
    "purpose": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "What the resource is meant to be used for, as free text. Answers a different question from `abstract`, which says what it contains. Null when the record states none.",
      "title": "Purpose"
    },
    "spatialScope": {
      "anyOf": [
        {
          "$ref": "#/$defs/SpatialScope"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Extent the resource is meant to describe, from the INSPIRE `SpatialScope` code list, read from the code the record cites and not from its label. Null when the record cites none, which is the majority of the catalogue."
    },
    "bbox": {
      "anyOf": [
        {
          "items": {
            "type": "number"
          },
          "type": "array"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Geographic extent as [west, south, east, north] in decimal degrees (WGS 84), in GeoJSON order. The union of every box in `extents`, which makes it a cheap first filter but a coarse one: a record covering mainland France and the overseas territories gets a box reaching from the Caribbean to Réunion, up to 3 519 times the area actually covered. Read `extents` before concluding that a resource covers a point. Null when the record declares no usable box.",
      "title": "Bbox"
    },
    "extents": {
      "description": "The boxes the record declares, one entry each, with the name and code published for them. An extent carrying no usable box — a purely temporal one, for instance — is not listed here. Empty when the record declares none.",
      "items": {
        "$ref": "#/$defs/Extent"
      },
      "title": "Extents",
      "type": "array"
    },
    "temporalStart": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Start of the temporal extent, ISO 8601, null when absent.",
      "title": "Temporalstart"
    },
    "temporalEnd": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "End of the temporal extent, ISO 8601, null when absent.",
      "title": "Temporalend"
    },
    "created": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Creation date of the resource, ISO 8601, null when absent.",
      "title": "Created"
    },
    "published": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Publication date of the resource, ISO 8601, null when absent.",
      "title": "Published"
    },
    "revised": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Last revision date of the resource, ISO 8601, null when absent.",
      "title": "Revised"
    },
    "updateFrequency": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "How often the resource is updated, as the `MD_MaintenanceFrequencyCode` value the record publishes, e.g. 'quarterly' or 'notPlanned'. Kept verbatim and not validated against the ISO code list, because the catalogue publishes 'quaterly' (sic) on one record and a misspelling is not something to repair silently. Null when the record states none.",
      "title": "Updatefrequency"
    },
    "lineage": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "How the resource was produced, as free text: sources, methods and the accuracy they imply. Null when the record states none, which includes the 103 records publishing an empty statement.",
      "title": "Lineage"
    },
    "licence": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Licence or use condition, as free text published by the catalogue (e.g. 'Licence Ouverte / Open License'). Null when the record states none.",
      "title": "Licence"
    },
    "accessConstraint": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Limitation on public access, as free text (e.g. 'Pas de restriction d'accès public selon INSPIRE'). Null when the record states none.",
      "title": "Accessconstraint"
    },
    "links": {
      "description": "Access endpoints, one per (type, url); the catalogue publishes one entry per layer, collected into `layers`. Empty when the record publishes none.",
      "items": {
        "$ref": "#/$defs/Link"
      },
      "title": "Links",
      "type": "array"
    },
    "thumbnailUrl": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "URL of a preview image of the resource, published as a browse graphic. Null when the record carries none.",
      "title": "Thumbnailurl"
    },
    "suspectedTest": {
      "default": false,
      "description": "True when the record looks like a test record published by mistake (e.g. titled 'test'). A heuristic, exposed so that a consumer can filter it out rather than having to rediscover it.",
      "title": "Suspectedtest",
      "type": "boolean"
    }
  },
  "required": [
    "fileIdentifier",
    "type"
  ],
  "title": "CatalogueRecord",
  "type": "object"
}
